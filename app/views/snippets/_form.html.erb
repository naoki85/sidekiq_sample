<%= form_with(model: snippet, local: true) do |form| %>
  <% if snippet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(snippet.errors.count, "error") %> prohibited this snippet from being saved:</h2>

      <ul>
      <% snippet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :language %>
    <%= form.text_field :language, id: :snippet_language %>
  </div>

  <div class="field">
    <%= form.label :plain_code %>
    <%= form.text_area :plain_code, id: :snippet_plain_code %>
  </div>

  <div class="field">
    <%= form.label :highlighted_code %>
    <%= form.text_area :highlighted_code, id: :snippet_highlighted_code %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
